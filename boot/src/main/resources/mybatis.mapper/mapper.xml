forEach: Aggregate
representativeFor: Aggregate
fileName: {{namePascalCase}}Mapper.xml
path: {{boundedContext.name}}/s20a01-boot/src/main/resources/mybatis.mapper
---

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.posco.{{boundedContext.name}}.s20a01.domain.mybatis.{{namePascalCase}}Mapper">
    
    <!-- 예시 -->
    <!-- <select id="select{{namePascalCase}}List" resultType="{{namePascalCase}}MybatisEntity">
        SELECT * FROM {{namePascalCase}}_table
    </select>
    <select id="select{{namePascalCase}}" resultType="{{namePascalCase}}MybatisEntity">
        SELECT * FROM {{namePascalCase}}_table WHERE {{keyFieldDescriptor.nameCamelCase}} = #{id}
    </select> -->
</mapper>